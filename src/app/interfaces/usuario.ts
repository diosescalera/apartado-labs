export interface Usuario {
        _id: string;
        idusuario: string;
        tipo: string;
        nombre: string;
        activo: boolean;
        createdAt: string;
        updatedAt: string;
}
